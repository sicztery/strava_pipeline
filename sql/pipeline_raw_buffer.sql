INSERT INTO `${PROJECT_ID}.${DATASET}.strava_buffer` (
  activity_id,
  name,
  sport_type,
  start_date_utc,
  start_date_local,
  utc_offset,
  timezone,
  distance_m,
  moving_time_s,
  elapsed_time_s,
  elevation_gain_m,
  average_speed_kmh,
  max_speed_kmh,
  average_hr,
  max_hr,
  has_heartrate,
  average_cadence,
  average_watts,
  max_watts,
  weighted_average_watts,
  kilojoules,
  suffer_score,
  device_name,
  gear_id,
  start_lat,
  start_lng,
  end_lat,
  end_lng,
  is_private,
  is_commute,
  is_manual,
  ingest_ts
)
SELECT
  activity_id,
  name,
  sport_type,
  start_date_utc,
  start_date_local,
  utc_offset,
  timezone,
  distance_m,
  moving_time_s,
  elapsed_time_s,
  elevation_gain_m,
  average_speed_kmh,
  max_speed_kmh,
  average_hr,
  max_hr,
  has_heartrate,
  average_cadence,
  average_watts,
  max_watts,
  weighted_average_watts,
  kilojoules,
  suffer_score,
  device_name,
  gear_id,
  start_lat,
  start_lng,
  end_lat,
  end_lng,
  is_private,
  is_commute,
  is_manual,
  ingest_ts
FROM `${PROJECT_ID}.${DATASET}.strava_raw_ext`;